import React from 'react';

const BlogDetails = ({params}) => {
    console.log(params);

    

    return (
        <div>
            BlogDetails
        </div>
    );
};

export default BlogDetails;